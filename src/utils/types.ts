export type YeeterItem = {
  id: string;
  createdAt: string;
  dao: {
    id: string;
  };
  endTime: string;
  startTime: string;
  isShares: boolean;
  multiplier: string;
  minTribute: string;
  maxTarget: string;
  balance: string;
};
